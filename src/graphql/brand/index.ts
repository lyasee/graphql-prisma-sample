import { mutation } from "./brand.mutation";
import { resolver } from "./brand.resolver";
import { query } from "./brand.query";

const module = {
  mutation,
  resolver,
  query,
};

export default module;
